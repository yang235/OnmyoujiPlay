# ============================================
# OnmyoujiPlay 依赖清单
# 源环境: conda oop (Python 3.8.20)
# ============================================

# ······· 一键安装 ········
#   pip install -r requirements.txt
#
#   或使用 conda 创建独立环境:
#   conda create -n onmyouji python=3.8 -y
#   conda activate onmyouji
#   pip install -r requirements.txt
# ························

# --- 核心依赖（项目直接 import） ---
opencv-contrib-python==4.10.0.84   # cv2: 模板匹配 + 图像处理
paddleocr==3.5.0                    # PaddleOCR: 文字识别
paddlepaddle==3.0.0                 # PaddlePaddle GPU 版 (paddlepaddle-gpu==3.0.0)
paddlex==3.5.2                      # PaddleX 工具集
pywin32==311                        # win32gui / win32ui / win32con: 窗口截取
pywinauto==0.6.9                    # 窗口操作
Pillow==10.4.0                      # PIL: 图像处理 (tkinter 显示)
PyAutoGUI==0.9.54                   # 鼠标/键盘自动化
keyboard==0.13.5                    # 键盘操作
numpy==1.24.4                       # 数组运算
pandas==2.0.3                       # Excel 账号信息读取
openpyxl==3.1.5                     # Excel 读写 (.xlsx)

# --- 隐式依赖（被上述包拉取，固定版本避免不兼容） ---
shapely==2.0.7
pyclipper==1.3.0.post6
python-bidi==0.6.7
comtypes==1.4.12
PyGetWindow==0.0.9
PyMsgBox==2.0.1
PyRect==0.2.0
PyScreeze==1.0.1
pytweening==1.2.0
pyperclip==1.11.0
MouseInfo==0.1.3
